<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mykotlinapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyKotlinApplication">
        <activity
            android:name=".Principal.logged"
            android:exported="true" />
        <activity
            android:name=".DataBase.Register2"
            android:exported="true"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.MyKotlinApplication.NoActionBar" />
        <activity
            android:name=".DataBase.MainActivity"
            android:exported="true" />
        <activity
            android:name=".Principal.SplashScreen"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>